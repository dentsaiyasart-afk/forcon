# 🌟 ระบบรับสมัครงานออนไลน์

ระบบรับสมัครงานออนไลน์แบบครบถ้วน พร้อมสร้าง PDF อัตโนมัติและส่งอีเมลยืนยัน

## ✨ คุณสมบัติหลัก

- ✅ ฟอร์มสมัครงานครบถ้วน (ข้อมูลส่วนตัว, การศึกษา, ประสบการณ์)
- ✅ อัปโหลดไฟล์เรซูเม่ (PDF, DOC, DOCX)
- ✅ อัปโหลดรูปถ่ายหน้าตรง (บังคับ)
- ✅ สร้าง PDF ใบสมัครอัตโนมัติ
- ✅ ส่งอีเมลยืนยันให้ผู้สมัคร
- ✅ ส่งอีเมลพร้อม PDF และไฟล์แนบให้ HR
- ✅ รองรับการกรอก "-" ในทุกช่อง
- ✅ ตรวจสอบเลขบัตรประชาชน 13 หลัก
- ✅ มีช่องกรอกโรคประจำตัว และประวัติอาชญากรรม
- ✅ รองรับเพศ "อื่นๆ"
- ✅ ระบุวุฒิการศึกษาที่ใช้สมัคร

---

## 📁 โครงสร้างไฟล์

```
job-application-system/
├── api/
│   └── index.js              # Serverless API
├── job-application.html      # หน้าฟอร์มสมัครงาน
├── package.json              # Dependencies
├── vercel.json               # Vercel config
├── .env                      # Environment variables (สร้างเอง)
├── .env.example              # ตัวอย่าง env
├── .gitignore               
└── README.md                 # คู่มือนี้
```

---

## 🚀 วิธีการติดตั้งและ Deploy

### ขั้นตอนที่ 1: เตรียมโปรเจค

1. **สร้างโฟลเดอร์โปรเจค**
```bash
mkdir job-application-system
cd job-application-system
```

2. **สร้างไฟล์ทั้งหมด**
   - สร้างโฟลเดอร์ `api/`
   - สร้างไฟล์ `api/index.js` (คัดลอกโค้ดจาก artifact)
   - สร้างไฟล์ `job-application.html` (คัดลอกโค้ดจาก artifact)
   - สร้างไฟล์ `package.json` (คัดลอกจาก artifact)
   - สร้างไฟล์ `vercel.json` (คัดลอกจาก artifact)
   - สร้างไฟล์ `.gitignore` (คัดลอกจาก artifact)

3. **ติดตั้ง Dependencies**
```bash
npm install
```

---

### ขั้นตอนที่ 2: ตั้งค่า Gmail App Password

**สำคัญมาก!** คุณต้องสร้าง App Password จาก Gmail

1. ไปที่ [Google Account Security](https://myaccount.google.com/security)
2. เปิดใช้งาน **2-Step Verification** ก่อน
3. ไปที่ [App Passwords](https://myaccount.google.com/apppasswords)
4. เลือก **Mail** และ **Other (Custom name)**
5. ตั้งชื่อว่า "Job Application System"
6. คลิก **Generate**
7. คัดลอก password 16 หลัก (ไม่มีเว้นวรรค)

**หมายเหตุ:** อย่าใช้รหัสผ่านจริงของ Gmail!

---

### ขั้นตอนที่ 3: สร้างไฟล์ .env

สร้างไฟล์ `.env` ในโฟลเดอร์หลัก:

```bash
# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=xxxx xxxx xxxx xxxx

# Admin Email (อีเมลที่จะได้รับใบสมัคร)
ADMIN_EMAIL=hr@company.com

# Optional
COMPANY_NAME=บริษัทของคุณ
```

**แทนที่:**
- `your-email@gmail.com` = Gmail ของคุณ
- `xxxx xxxx xxxx xxxx` = App Password 16 หลักจากขั้นตอนที่ 2
- `hr@company.com` = อีเมล HR ที่จะได้รับใบสมัคร

---

### ขั้นตอนที่ 4: ทดสอบในเครื่อง (Local)

1. **ติดตั้ง Vercel CLI**
```bash
npm install -g vercel
```

2. **รัน Local Server**
```bash
vercel dev
```

3. **เปิดเบราว์เซอร์**
```
http://localhost:3000
```

4. **ทดสอบส่งฟอร์ม** เพื่อดูว่าทุกอย่างทำงานได้

---

### ขั้นตอนที่ 5: Deploy ไปยัง Vercel

#### วิธีที่ 1: Deploy ด้วย Vercel CLI (แนะนำ)

1. **Login Vercel**
```bash
vercel login
```

2. **Deploy**
```bash
vercel
```
   - กด Enter เพื่อยอมรับทุกอย่าง
   - จะได้ URL ชั่วคราว เช่น `your-project-xyz.vercel.app`

3. **Deploy Production**
```bash
vercel --prod
```
   - จะได้ URL production

---

#### วิธีที่ 2: Deploy ผ่าน Vercel Dashboard

1. **สร้าง Git Repository**
```bash
git init
git add .
git commit -m "Initial commit"
```

2. **Push ไปยัง GitHub**
   - สร้าง repository ใหม่บน GitHub
   - Push โค้ด:
```bash
git remote add origin https://github.com/username/job-app.git
git branch -M main
git push -u origin main
```

3. **Import ไปยัง Vercel**
   - ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
   - คลิก **"New Project"**
   - เลือก repository ของคุณ
   - คลิก **"Import"**

4. **ตั้งค่า Environment Variables**
   - ในหน้า Project Settings
   - ไปที่ **"Environment Variables"**
   - เพิ่มตัวแปรจากไฟล์ `.env`:
     ```
     SMTP_HOST = smtp.gmail.com
     SMTP_PORT = 587
     EMAIL_USER = your-email@gmail.com
     EMAIL_PASS = xxxx xxxx xxxx xxxx
     ADMIN_EMAIL = hr@company.com
     COMPANY_NAME = บริษัทของคุณ
     ```

5. **Deploy**
   - คลิก **"Deploy"**
   - รอ 1-2 นาที

---

## 🎯 วิธีการใช้งาน

### สำหรับผู้สมัครงาน

1. เปิดเว็บ `https://your-project.vercel.app`
2. กรอกข้อมูลในฟอร์มให้ครบถ้วน
3. อัปโหลดไฟล์:
   - รูปถ่ายหน้าตรง (บังคับ)
   - เรซูเม่ (ถ้ามี)
4. คลิก "ส่งใบสมัครงาน"
5. จะได้รับอีเมลยืนยันพร้อมรหัสใบสมัคร

### สำหรับ HR/Admin

1. จะได้รับอีเมลแจ้งเตือนทันที
2. อีเมลจะมีไฟล์แนบ:
   - PDF ใบสมัครงานฉบับเต็ม
   - รูปถ่ายหน้าตรงของผู้สมัคร
   - ไฟล์เรซูเม่ (ถ้ามี)
3. ตรวจสอบข้อมูลและติดต่อกลับผู้สมัคร

---

## 🔧 การแก้ไขและปรับแต่ง

### เปลี่ยนสีธีม

แก้ไขใน `job-application.html`:
```css
/* หาบรรทัดนี้ */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* เปลี่ยนเป็นสีที่ต้องการ */
background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%);
```

### เพิ่มตำแหน่งงาน

แก้ไขใน `job-application.html`:
```html
<!-- เพิ่มตำแหน่งใหม่ -->
<div class="checkbox-item">
    <input type="checkbox" id="pos_new" name="position" value="ตำแหน่งใหม่">
    <label for="pos_new">🎯 ตำแหน่งใหม่</label>
</div>
```

### เปลี่ยนข้อความอีเมล

แก้ไขใน `api/index.js`:
- ค้นหา `applicantEmailHTML` = อีเมลส่งถึงผู้สมัคร
- ค้นหา `adminEmailHTML` = อีเมลส่งถึง HR

---

## 🐛 การแก้ปัญหา

### ปัญหา: ส่งอีเมลไม่ได้

**สาเหตุ:**
- App Password ไม่ถูกต้อง
- 2-Step Verification ยังไม่เปิด
- SMTP ถูกบล็อค

**วิธีแก้:**
1. ตรวจสอบ App Password อีกครั้ง
2. ตรวจสอบว่าเปิด 2-Step Verification แล้ว
3. ลองใช้ SMTP อื่น (Outlook, SendGrid)

---

### ปัญหา: อัปโหลดไฟล์ไม่ได้

**สาเหตุ:**
- ไฟล์ใหญ่เกิน 5MB
- ประเภทไฟล์ไม่ถูกต้อง

**วิธีแก้:**
1. ลดขนาดไฟล์ให้น้อยกว่า 5MB
2. ตรวจสอบว่าเป็น PDF, DOC, DOCX, JPG, PNG

---

### ปัญหา: PDF ไม่มีภาษาไทย

**วิธีแก้:**
ต้องใช้ฟอนต์ที่รองรับภาษาไทย ในไฟล์ `api/index.js`:

```javascript
// เพิ่มฟอนต์ภาษาไทย (ต้องมีไฟล์ฟอนต์)
doc.registerFont('THSarabunNew', './fonts/THSarabunNew.ttf');
doc.font('THSarabunNew');
```

**หมายเหตุ:** การใช้ฟอนต์ใน Vercel Serverless ซับซ้อน แนะนำให้ใช้ภาษาอังกฤษใน PDF หรือเปลี่ยนเป็น HTML email แทน

---

## 📊 ข้อมูลที่เก็บ

ระบบจะส่งข้อมูลทั้งหมดไปที่อีเมล HR ไม่มีฐานข้อมูล

**ข้อมูลที่เก็บใน PDF:**
- ข้อมูลส่วนตัว (ชื่อ, เพศ, วันเกิด, อายุ)
- สัญชาติ, เชื้อชาติ, ศาสนา
- ข้อมูลติดต่อ (เบอร์, LINE, อีเมล, ที่อยู่)
- เลขบัตรประชาชน 13 หลัก
- ประวัติการศึกษา
- วุฒิการศึกษาที่ใช้สมัคร
- ประสบการณ์ทำงาน
- โรคประจำตัว (ถ้ามี)
- ประวัติอาชญากรรม (ถ้ามี)
- ทักษะพิเศษ
- เงินเดือนที่คาดหวัง

**ไฟล์แนบ:**
- PDF ใบสมัคร (สร้างอัตโนมัติ)
- รูปถ่ายหน้าตรง
- เรซูเม่ (ถ้ามี)

---

## 🔐 ความปลอดภัย

- ✅ ข้อมูลส่งผ่าน HTTPS
- ✅ ไม่เก็บข้อมูลในฐานข้อมูล
- ✅ ส่งข้อมูลผ่านอีเมลเข้ารหัส
- ⚠️ อย่า commit ไฟล์ `.env` ลง Git
- ⚠️ ใช้ Environment Variables ใน Vercel

---

## 📝 License

MIT License - ใช้งานได้ฟรี แก้ไขได้ตามใจชอบ

---

## 🙏 ขอบคุณ

สร้างด้วย ❤️ โดยใช้:
- Express.js
- PDFKit
- Nodemailer
- Vercel Serverless Functions

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย สามารถสอบถามได้

**สำเร็จแล้ว! 🎉**